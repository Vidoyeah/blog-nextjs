const { Fragment } = require('react');

import Hero from '@/components/home-page/hero';

function HomePage() {
  return (
    <Fragment>
      <Hero />
    </Fragment>
  );
}
export default HomePage;
